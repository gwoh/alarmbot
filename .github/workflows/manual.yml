name: Manual workflow

on:
  workflow_dispathch:

jobs:
  message:
  runs-on: ubuntu-latest
  steps:
  - name: Discord Webhook Action
    uses: tsickert/discord-webhook@v0.0.7
    with:
      webhook-url: ${{ secrets.WEBHOOK_URL }}
      content: "message from github action"
